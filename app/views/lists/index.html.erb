<h1>My Lists</h1>
<ul>
  <% @lists.each do |l|  %>
    <li>
      <%= link_to l.name, list_path(l) %>
    </li>
  <% end %>
</ul>
<%= link_to "Create a movie list", new_list_path, class: 'btn btn-primary'%>
